# Install only inside the project venv — see README.md (Isolated Python environment).
fastapi
uvicorn
pydantic
pydantic-settings
httpx
pandas
yfinance
python-dotenv
lxml
numpy
# ── LLM providers ─────────────────────────────────────────────────────────────
# Gemini, Groq (LLaMA), and OpenRouter (Mistral) are all called via httpx REST.
# No extra SDK packages required. FinBERT also uses the HF Inference REST API.
# Add these keys to backend/.env:
#   GEMINI_API_KEY=...
#   GROQ_API_KEY=...
#   OPENROUTER_API_KEY=...
#   HUGGINGFACE_API_TOKEN=...   (for FinBERT sentiment + RAG embeddings)
