# Install only inside the project venv — see README.md (Isolated Python environment).
fastapi
uvicorn
pydantic
pydantic-settings
httpx
pandas
yfinance
python-dotenv
lxml
numpy
eval_type_backport  # Python 3.9 + Pydantic v2 compatibility for X|None syntax
pytest
respx              # httpx mock transport for integration tests
matplotlib         # evaluation bar charts (plot_model_metrics_bars.py)
# ── Auth ──────────────────────────────────────────────────────────────────────
python-jose[cryptography]  # JWT signing / verification
passlib[bcrypt]            # password hashing
bcrypt<4.0                 # passlib 1.7.4 is incompatible with bcrypt >= 4.0
# ── LLM providers ─────────────────────────────────────────────────────────────
# Gemini and Ollama (LLaMA / Mistral) use httpx REST. Buy/Sell HF instruct models may use
# huggingface_hub.InferenceClient chat (router / provider ids like Qwen/...:featherless-ai).
huggingface_hub>=0.26.0
# Add these keys to backend/.env:
#   GEMINI_API_KEY=...
#   HUGGINGFACE_API_TOKEN=...   (for FinBERT sentiment + RAG embeddings)
