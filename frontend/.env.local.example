# Base URL of the FastAPI backend (no trailing slash).
# Must match where uvicorn listens (default 8000). If the Next dev server uses another port,
# ensure backend CORS (cors_origins in backend .env) includes that origin.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
